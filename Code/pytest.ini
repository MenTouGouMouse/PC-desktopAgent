[pytest]
markers =
    integration: layer-to-layer integration tests (deselect with -m "not integration")
    e2e: full end-to-end tests, skipped by default (deselect with -m "not e2e")
tmp_path_retention_policy = none
tmp_path_retention_count = 0

